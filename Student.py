class Student:
    # Fields
    def __init__(self, p, c, n, e, f):
        self.preferences = p
        self.course = c
        self.name = n
        self.email = e
        self.form = f

    # Calculates the utility of a student
    # Higher utility means that the index
    def utility():
        return 6 - p.index(c)
